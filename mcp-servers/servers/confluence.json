{
  "type": "stdio",
  "command": "node",
  "args": [
    "${workspaceFolder}/orgwave/mcp-confluence-runtime/node_modules/@answerai/confluence-mcp/build/index.js"
  ],
  "env": {
    "CONFLUENCE_BASE_URL": "https://confluence.myntracorp.com/"
  },
  "envFile": "${workspaceFolder}/.env",
  "_orgwave": {
    "title": "Confluence",
    "purpose": "Create and update Confluence pages (e.g. playbook confluence-service-doc).",
    "env": [
      "CONFLUENCE_BASE_URL — pinned in this file (https://confluence.myntracorp.com/).",
      "CONFLUENCE_USER_EMAIL — set in repo-root `.env` (envFile).",
      "CONFLUENCE_API_TOKEN — Confluence personal access token or API token; set in repo-root `.env`. Same file as GitHub PAT is fine."
    ],
    "permissionsAllowlistHint": "confluence:*"
  }
}

# id = folder under playbooks/ — add a row and matching playbooks/<id>/SKILL.md (this file lives in orgwave/)

playbooks:
  - id: example-migration
    name: Example migration (POC)
    description: Sample flow — marker file + PR; copy _template for real migrations.
  - id: readme-cursor-smoke-test
    name: README Cursor smoke test
    description: Test flow — list repos via GitHub MCP (search); gh/git for README branch+push; MCP create_pull_request; pick targets, one PR per repo.
